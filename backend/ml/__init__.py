"""
Music-information-retrieval (MIR) wrappers.
All of these use pre-trained models -- no training needed.

Flow: audio file --> features (chords, beats, key, stems, MIDI) --> feed to LLM chains.
"""
