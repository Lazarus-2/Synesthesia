# Generated compatibility shim for Plan 2 H3.
# Source of truth is now ``pyproject.toml`` (PEP 621). Regenerate this file
# with ``pip-compile --extra dev pyproject.toml -o requirements.txt`` (or
# the ``uv`` equivalent) whenever you change deps. Existing CI / Docker
# workflows that ``pip install -r requirements.txt`` continue to work.

# === Web Framework ===
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# === Config ===
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# === LangChain ===
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=1.4.0
langchain-google-genai>=4.0.0
langchain-community>=0.3.0
langgraph>=0.2.0
langgraph-checkpoint-mongodb>=0.4.0

# === Vector Store (pick one) ===
faiss-cpu>=1.9.0

# === Audio Processing ===
ffmpeg-python>=0.2.0
pydub>=0.25.1
librosa>=0.10.2
soundfile>=0.12.1
# 2026 yt-dlp requires the EJS challenge solver + a JS runtime (Deno or Node).
# pip can't install either — install Deno separately (`npm i -g deno-bin` works
# without sudo) OR pass `--js-runtimes node:$(which node)` to yt-dlp at runtime.
yt-dlp>=2026.03.13

# === Music platform integrations (Plan v2) ===
# Spotify Web API audio-features is dead for new apps as of Nov 2024 + Feb 2026,
# so we use spotipy only for track metadata + ToS-clean iframe embed playback.
# Deezer (no auth) + MusicBrainz handle search; LRCLIB handles synced lyrics;
# AcoustID enriches uploads with MBID (degrades gracefully if `fpcalc` missing).
spotipy>=2.24.0
musicbrainzngs>=0.7.1
pyacoustid>=1.3.1
# Bundled static ffmpeg wheel — lets the worker run on machines without
# system ffmpeg (CI containers, fresh dev boxes). We still prefer
# system ffmpeg if `which ffmpeg` finds one.
imageio-ffmpeg>=0.5.1

# === Music theory analysis ===
music21>=10.3.0

# === Music ML Models ===
# All pre-trained, no training needed
# madmom>=0.16.1  # WARN: unmaintained since 2018; we keep librosa fallback
# basic-pitch>=0.4.0
# demucs is heavy -- uncomment if running stem separation locally
# demucs>=4.0.1
# autochord>=0.1.2  # optional simpler alternative to madmom

# === Token counting ===
tiktoken>=0.8.0

# === Async & Caching ===
redis>=5.2.0
taskiq>=0.11.0
taskiq-redis>=0.5.0
taskiq-fastapi>=0.3.0
slowapi>=0.1.9
sse-starlette>=2.1.0

# === Auth (Plan 2 D4, opt-in) ===
PyJWT>=2.9.0
bcrypt>=4.2.0

# === Observability (Plan 2 H1) ===
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
# celery>=5.4.0  # uncomment for async task queue

# === Testing ===
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# === Dev ===
ipython>=8.29.0
